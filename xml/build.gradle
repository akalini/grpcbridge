apply plugin: 'grpcbridge.gradle.publish'

archivesBaseName = 'grpcbridge-xml'

dependencies {
    implementation project(":lib")
    implementation 'com.fasterxml.jackson.dataformat:jackson-dataformat-xml:2.9.6'
}
